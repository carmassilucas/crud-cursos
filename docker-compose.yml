version: '3.9'

services:
  postgres:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: "crudcursospostgrespass"
      POSTGRES_USER: "crudcursospostgresuser"
      POSTGRES_DB: "crud-cursos"